name: Publicar painel da ONG (web)

# Publica o PAINEL DA ONG como site estatico no GitHub Pages, para abrir em
# QUALQUER maquina (ex.: o PC da escola) sem instalar Flutter nem clonar nada.
#
# Endereco final: https://gabriel-chinelatto.github.io/connect-ong-desktop/
#
# Detalhes que importam:
# - `--base-href /connect-ong-desktop/`: o Pages serve o site numa SUBPASTA com
#   o nome do repositorio. Sem isso o app carrega em branco.
# - Modo Feira fica LIGADO (padrao do app): a tela de login exibe as credenciais
#   de demonstracao. Para esconder, compilar com
#   `--dart-define=MODO_FEIRA_PADRAO=false` ou desligar nas Configuracoes.
# - A API usada e a padrao (Render). Esse endereco precisa estar liberado no
#   CORS do backend (ver SecurityConfig.allowedOrigins).

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Baixar dependencias
        run: flutter pub get

      - name: Conferir analise estatica
        run: flutter analyze

      - name: Rodar testes
        run: flutter test

      # MODO FEIRA LIGADO tambem na versao publicada (decisao do usuario em
      # 05/08/2026): o card de credenciais no login e o que permite o visitante
      # do estande entrar sozinho. Contrapartida conhecida: quem abrir o link
      # enxerga um login valido de ONG e pode alterar os dados da demonstracao.
      - name: Compilar versao web
        run: |
          flutter build web --release \
            --base-href /connect-ong-desktop/

      - name: Tratar rotas profundas (404 -> index)
        run: cp build/web/index.html build/web/404.html

      - name: Configurar Pages
        uses: actions/configure-pages@v5
        with:
          # Liga o GitHub Pages do repositorio na primeira execucao (sem isto
          # seria preciso habilitar na mao em Settings > Pages).
          enablement: true

      - name: Enviar artefato
        uses: actions/upload-pages-artifact@v3
        with:
          path: build/web

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Publicar no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
